local palette = require(script.Parent.Palette)

return {
    background = palette.grey[2],
    onBackground = palette.grey[8],

    surface = palette.grey[3],
    onSurface = palette.grey[8],

    primary = palette.orange[0],
    onPrimary = palette.grey[8],

    secondary = palette.magenta[0],
    onSecondary = palette.grey[8],
}