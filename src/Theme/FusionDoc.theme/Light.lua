local palette = require(script.Parent.Palette)

return {
    background = palette.grey[8],
    onBackground = palette.grey[2],

    surface = palette.grey[9],
    onSurface = palette.grey[2],

    primary = palette.orange[0],
    onPrimary = palette.grey[2],

    secondary = palette.magenta[0],
    onSecondary = palette.grey[2],
}